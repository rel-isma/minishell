ft_get_path()
    if already a path return it
        /dddd > path = /dddd        #execve

    if access returned 1
        ls > path = /bin/ls         #execve
    else
        lsss > path = NULL      #command not found

heredoc close and open



//////////
echo $_
cd: home ~ ////

exit();
// minishell$ cat << $'t'est
// > $test 
// minishell$ cat << $'t'est
// > test 
/// $"USER" \\ USER
if(!env) export SEGV

export = // should give  `=': not a valid identifier
cd with relative path
minishell$ $asads // bash-3.2$ $dsd
minishell: : command not found // bash-3.2$ 