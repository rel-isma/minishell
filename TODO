ft_get_path()
    if already a path return it
        /dddd > path = /dddd        #execve

    if access returned 1
        ls > path = /bin/ls         #execve
    else
        lsss > path = NULL      #command not found

heredoc close and open



//////////
echo $_
SHLVL // if not in the env
cd: home ~ ////

exit();
minishell$ export | grep SHLVL
minishell: export: command not found
// minishell$ cat << $'t'est
// > $test 
// minishell$ cat << $'t'est
// > test 
/// $"USER" \\ USER